<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x"
      crossorigin="anonymous"
    />
    <link rel="preconnect" href="https://fonts.gstatic.com" />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto&display=swap"
      rel="stylesheet"
    />
    <title>Verifikasi</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        font-family: "Roboto";
      }
      html {
        overflow-x: hidden;
      }
      /* .tombol {
        background-color: #f72585;
        color: white;
      } */
      .container{
          width: 75%;
      }
    </style>
  </head>
  <body>
    <!-- navbar -->
    <nav class="navbar btn-dark navbar-expand-xs">
      <div class="container-fluid">
        <!-- <a href="/" style="text-decoration: none"
          ><h3 style="color: white">IPB Peduli</h3></a
        > -->
        <img class="ms-4" src="/images/logo.png" alt="logo" height="40px" />

        <div class="d-flex">
          <div>
            <a class="btn" style="color: white" href="#">Riwayat</a>
          </div>
          <div>
            <a class="btn" style="color: white" href="#">Tentang</a>
          </div>
          <div>
            <a class="btn" style="color: white" href="/list">Galang dana</a>
          </div>
        </div>

        <div class="d-flex me-5">
          <% if(!currentUser) { %>
          <a href="/login" class="btn btn-success">Login</a>
          <a href="/register" class="btn btn-secondary">Register</a>
          <% } else { %>
          <a href="/logout" class="btn border border-0 btn-primary tombol"
            >Logout</a
          >
          <% } %>
        </div>
      </div>
    </nav>

    <!-- container -->

    <div class="container mt-5">
            <h3>Verifikasi <span class="text-danger"><%= item.question1 %> - <strong><%= item.creator.username %> </strong></span></h3>
        <hr>
        <h5><strong>Dokumen validasi campaign</strong></h5>
            <!-- isi komponen dokumen 
            .
            .
            .
            -->
            <a href="/" target="_blank">dokumen bencana.pdf</a>

        <div class="form-check mb-3">
            <input
              required
              class="form-check-input"
              type="checkbox"
              value=""
              id="flexCheckDefault"
            />
            <label class="form-check-label" for="flexCheckDefault">
              Verifikasi bahwa dokumen diatas sudah benar
            </label>
          </div>
          <hr>
          <h5><strong>Dokumen Persyaratan Pengambilan Uang</strong></h5>
            <a href="/" target="_blank">ktm.pdf</a>
            <br>
            <a href="/" target="_blank">dokumen ditmawa.pdf</a>

          <div class="form-check">
            <input
              required
              class="form-check-input"
              type="checkbox"
              value=""
              id="flexCheckDefault"
            />
            <label class="form-check-label" for="flexCheckDefault">
              Verifikasi bahwa dokumen diatas sudah benar
            </label>
          </div>
          <div style="text-align: end;">
            <form action="/delete/<%= item._id %>?_method=DELETE" method="POST" style="display: inline-block;">
              <button class="btn-lg btn-danger border border-0 tombol">
                Decline
              </button>
            </form>
            <form action="/admin/<%= item._id %>?_method=PATCH" method="POST" style="display: inline-block;">
              <button class="btn-lg btn-primary border border-0">
                Accept
              </button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
